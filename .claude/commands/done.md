# 브랜치 정리

PR을 올린 후 현재 feature 브랜치를 삭제하고 dev 브랜치로 이동합니다.

## 작업 순서

1. 현재 브랜치 이름을 확인합니다
2. 현재 브랜치가 dev 또는 master인 경우 작업을 중단하고 사용자에게 알립니다
3. dev 브랜치로 checkout합니다
4. origin/dev 기준으로 pull합니다
5. 이전 feature 브랜치를 로컬에서 삭제합니다

## 실행

위 순서대로 git 명령어를 실행합니다.
